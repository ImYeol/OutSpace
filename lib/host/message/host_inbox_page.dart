import 'package:flutter/material.dart';

class HostInboxPage extends StatelessWidget {
  const HostInboxPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
