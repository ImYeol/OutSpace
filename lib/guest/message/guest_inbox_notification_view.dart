import 'package:flutter/material.dart';

class GuestInboxNotificationView extends StatelessWidget {
  const GuestInboxNotificationView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
