import 'package:flutter/material.dart';

class GuestInboxMessageView extends StatelessWidget {
  const GuestInboxMessageView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
